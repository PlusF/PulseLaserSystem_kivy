#:kivy 1.0.9

<MainWindow>:
    BoxLayout:
        center_x: root.width / 2
        center_y: root.height / 2
        orientation: "vertical"

        Button:
            id: button_top
            text:
            on_press:
            on_release:

        Button:
            id: button_bottom
            text:
            on_press:
            on_release:

        Button:
            id: button_left
            text:
            on_press:
            on_release:

        Button:
            id: button_right
            text:
            on_press:
            on_release:
